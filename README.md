აბსოლუტურად გასაგებია 💪 —
გინდა, რომ GitHub-ის პროექტის სახელი იყოს **`hyBackup`**,
მაგრამ სკრიპტები დარჩეს მარტივად მოხმარებადი — `backup.sh` და `restore.sh`.

ქვემოთ გიწერ **სრულად გამართული, სუფთა და დოკუმენტურად ზუსტ README.md**
შენი რეპოზიტორის სტრუქტურისთვის:
`~/hyBackup` დირექტორიაში, ფაილებით:
`backup.sh`, `restore.sh`, `.backup.list`, `README.md`, `.gitignore`.

---

# 🖥️ hyBackup

> Lightweight configuration backup and restore system for Manjaro + Hyperland
> ავტორი: **avto**
> ვერსია: 1.0

---

## 📦 აღწერა

**hyBackup** — ეს არის მარტივი, script-ებზე დაფუძნებული ბექაფის სისტემა,
შექმნილი შენი გარემოს (მაგ. Hyperland, Manjaro) ხელით მორგებული კონფიგურაციების
სწრაფად შესანახად და აღსადგენად.

🎯 ძირითადი იდეა:
ინახე მხოლოდ ის, რაც მართლა შენით გაქვს მორგებული —
არა ბრაუზერები, არა cache, არა IDE-ს GUI პარამეტრები.

---

## 📂 სტრუქტურა

```
hyBackup/
 ├── backup.sh          # ბექაფის სკრიპტი
 ├── restore.sh         # აღდგენის სკრიპტი
 ├── .backup.list       # ბექაფის ობიექტების სია
 ├── .gitignore
 └── README.md
```

---

## ⚙️ ინსტალაცია

```bash
git clone git@github.com:avtotrainer/hyBackup.git ~/hyBackup
cd ~/hyBackup
chmod +x backup.sh restore.sh
```

(სასურველია `~/.backup/` ფოლდერი არსებობდეს.
თუ არა — სკრიპტი ავტომატურად შექმნის მას.)

---

## 🚀 გამოყენება

### 📦 ბექაფის შექმნა

```bash
cd ~/hyBackup
./backup.sh
```

შეიქმნება ფაილი:

```
~/.backup/backup-YYYY-MM-DD_HH-MM-SS.tar.gz
```

👉 ბექაფში მოხვდება მხოლოდ `.backup.list`-ში ჩამოთვლილი ფაილები და დირექტორიები.

---

### 🔁 აღდგენა

```bash
cd ~/hyBackup
./restore.sh
```

👉 გაჩვენებს მენიუს ყველა შესაძლო აღდგენის ვარიანტით
(ან შეგიძლია მიუთითო არგუმენტებით, მაგალითად `./restore.sh ~/.backup/backup-latest.tar.gz 4`).

---

## 🧩 `.backup.list`

ეს ფაილი განსაზღვრავს ზუსტად რა კონფიგურაციები უნდა მოხვდეს ბექაფში.
შენ შეგიძლია მარტივად დაამატო ან წაშალო ხაზები.

მაგალითი:

```bash
# --- Shell setup ---
.zshrc
.p10k.zsh
.oh-my-zsh

# --- Terminal ---
.config/alacritty

# --- Developer tools ---
.gitconfig
.ssh

# --- Optional additions ---
#.tmux.conf
#.config/wezterm

# --- System info sources ---
/etc/fstab
/etc/NetworkManager
```

💡

* მხოლოდ uncomment-ებული ხაზები ჩაერთვება ბექაფში
* შეგიძლია შექმნა რამდენიმე სია, მაგ. `.backup.minimal`, `.backup.full`
  და გაუშვა:

```bash
./backup.sh .backup.minimal
```

---

## ⚡ Restore რეჟიმები (Option Numbers)

| რიცხვი | მოქმედება                              |
| ------ | -------------------------------------- |
| `1`    | მხოლოდ `.config` და `.local/share`     |
| `2`    | მხოლოდ Zsh კონფიგურაციები              |
| `3`    | მხოლოდ SSH გასაღებები                  |
| `4`    | სრული აღდგენა (გარდა `/etc` და Neovim) |
| `5`    | GitHub პროექტების auto-clone           |
| `6`    | Inspect mode (ნახვა არქივის შინაარსის) |
| `7`    | მხოლოდ `post-install.md` ამოღება       |
| `8`    | `/etc` ფაილების ამოღება                |
| `9`    | Preview restore (დრაი-რანი)            |
| `10`   | Clone/update Neovim config GitHub-დან  |

---

### 📘 გამოყენების მაგალითები

**სრული აღდგენა:**

```bash
./restore.sh ~/.backup/backup-latest.tar.gz 4
```

**Neovim კონფიგის clone GitHub-დან:**

```bash
./restore.sh ~/.backup/backup-latest.tar.gz 10
```

**SSH გასაღებები:**

```bash
./restore.sh ~/.backup/backup-latest.tar.gz 3
```

**Preview (არაფერს ცვლის, მხოლოდ აჩვენებს):**

```bash
./restore.sh ~/.backup/backup-latest.tar.gz 9
```

---

## 🧠 Neovim რეპოზიტორი

შენი `.zshrc` ფაილში უნდა გქონდეს მითითებული:

```bash
export NVIM_CONFIG_REPO="git@github.com:avtotrainer/nvchad-2.5-config.git"
```

---

## 💡 Appendix: Minimal Developer Setup Example

```bash
.zshrc
.p10k.zsh
.oh-my-zsh
.config/alacritty
.gitconfig
.ssh
```

📦 ამ სიის ბექაფი საშუალოდ 3–6MB იქნება,
საკმარისი იმისთვის, რომ შენი სამუშაო გარემო ზუსტად აღდგეს
ახალ სისტემაზე რამდენიმე წუთში.

---

## 🧹 Git Ignore (რეკომენდირებული)

```bash
*.tar.gz
session-*
/tmp/
/.cache/
/home/*/.backup/
```

---

## ✅ შეჯამება

| ამოცანა        | ფაილი          | შედეგი                        |
| -------------- | -------------- | ----------------------------- |
| ბექაფის შექმნა | `backup.sh`    | `.tar.gz` არქივი ~/.backup-ში |
| აღდგენა        | `restore.sh`   | შერჩეული აღდგენის ტიპით       |
| კონფიგების სია | `.backup.list` | განსაზღვრავს ბექაფის შინაარსს |

---

## 🧭 შენიშვნა

hyBackup:

* არ ბექაფავს ბრაუზერებს, IDE-ებს და cache ფოლდერებს
* ინახავს მხოლოდ შენს ხელით მორგებულ გარემოს
* იდეალურია სუფთა, სწრაფი და გადატანადი კონფიგურაციისთვის

---

გინდა, რომ README-ს ბოლოში დავამატო „📜 Quick Setup Script“ სექცია —
პატარა `install.sh`, რომელიც ავტომატურად დაამატებს PATH-ში და ალიასებს `backup`/`restore` ბრძანებებისთვის?
